---
// Sección de contacto
---
<section id="contacto" class="contact-section">
  <div class="contact-container">
  <!-- Eliminado decorativo verde -->
    <div class="contact-content">
      <h2 class="contact-title">¿Listo para reservar?</h2>
      <p class="contact-subtitle">Contáctanos para más información o realiza tu reserva ahora mismo. ¡Nuestro equipo está listo para ayudarte!</p>
      <form class="contact-form" autocomplete="off">
        <input type="text" placeholder="Nombre" required class="contact-input" aria-label="Nombre" />
        <input type="email" placeholder="Correo electrónico" required class="contact-input" aria-label="Correo electrónico" />
        <textarea placeholder="Mensaje o detalles de la reserva" required class="contact-textarea" aria-label="Mensaje o detalles de la reserva"></textarea>
        <button type="submit" class="contact-button">Enviar</button>
      </form>
    </div>
  </div>
</section>
<style>
.contact-section {
  position: relative;
  width: 100%;
  background: linear-gradient(135deg, var(--color-primary) 0%, #e6fcf7 100%);
  color: var(--color-background);
  padding: 5rem 1rem 6rem 1rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  overflow: hidden;
}


.contact-container {
  position: relative;
  max-width: 600px;
  width: 100%;
  z-index: 2;
  display: flex;
  flex-direction: column;
  align-items: center;
  animation: fadeInUp 0.7s ease forwards;
}

.contact-content {
  background: #fff;
  color: var(--color-dark);
  border-radius: 1.5rem;
  box-shadow: 0 8px 32px rgba(0,171,145,0.10);
  padding: 2.8rem 2.2rem 2.2rem 2.2rem;
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
}

.contact-title {
  font-size: 2.2rem;
  font-weight: 900;
  color: var(--color-primary);
  margin-bottom: 1.2rem;
  text-align: center;
  letter-spacing: -1px;
  background: linear-gradient(135deg, var(--color-primary) 0%, #00b894 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.contact-subtitle {
  font-size: 1.13rem;
  margin-bottom: 2.2rem;
  text-align: center;
  max-width: 420px;
  color: #444;
  line-height: 1.6;
}

.contact-form {
  display: flex;
  flex-direction: column;
  gap: 1.2rem;
  width: 100%;
}

.contact-input,
.contact-textarea {
  padding: 1rem 1.2rem;
  border-radius: 0.9rem;
  border: 1.5px solid #e6e6e6;
  outline: none;
  font-size: 1.08rem;
  background: #f8f8f8;
  transition: border-color 0.3s, box-shadow 0.3s;
  color: var(--color-dark);
}

.contact-input:focus,
.contact-textarea:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 2px rgba(0,171,145,0.10);
}

.contact-textarea {
  min-height: 90px;
  resize: vertical;
}

.contact-button {
  background: linear-gradient(90deg, var(--color-primary), #00b894);
  color: var(--color-background);
  padding: 1rem 0;
  border-radius: 1.1rem;
  font-size: 1.15rem;
  font-weight: 700;
  border: none;
  cursor: pointer;
  margin-top: 0.5rem;
  box-shadow: 0 2px 8px rgba(0,171,145,0.10);
  transition: background 0.2s, transform 0.2s;
  width: 100%;
}

.contact-button:hover,
.contact-button:focus {
  background: linear-gradient(90deg, #00b894, var(--color-primary));
  transform: translateY(-2px) scale(1.03);
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 768px) {
  .contact-section {
    padding: 4rem 1rem 5rem 1rem;
  }
  .contact-content {
    padding: 2rem 1rem 1.5rem 1rem;
  }
  .contact-title {
    font-size: 1.5rem;
  }

}

@media (max-width: 480px) {
  .contact-title {
    font-size: 1.1rem;
  }
  .contact-content {
    padding: 1.2rem 0.5rem 1rem 0.5rem;
  }
}
</style>
